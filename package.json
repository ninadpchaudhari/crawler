{
  "name": "dm-nl-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "DM vs NL reachability crawler for chat-assisted ecommerce",
  "scripts": {
    "setup": "npx playwright install chromium",
    "crawl": "node crawl.js",
    "crawl:headless": "node crawl.js --headless"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "pptxgenjs": "^4.0.1"
  }
}
